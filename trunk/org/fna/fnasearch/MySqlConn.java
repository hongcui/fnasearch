package org.fna.fnasearch;

/**
 * @author Kenneth
 *
 */

public class MySqlConn {
	/*
	 * A simple MySqlConn class that handles the connection and disconnection with MySqlDB;
	 * it also handles the query and return the result in list.
	 * 
	 * Return result set for Select
	 * Return number of rows affected for update,insert,delete
	 * Return boolean for create, drop etc.
	 */
}
